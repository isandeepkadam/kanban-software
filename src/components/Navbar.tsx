import { ReactElement } from "react"

const Navbar = (): ReactElement => {
  return (
    <ul className="navbar">
      <li>Home</li>
      <li>Search</li>
      <li>Login</li>
    </ul>
  )
}

export default Navbar
